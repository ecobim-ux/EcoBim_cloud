// Stub for the "server-only" import guard, aliased in here for the test
// environment only (see vitest.config.ts). Its real implementation just
// throws if bundled into client code — meaningless outside Next's bundler.
export {};
